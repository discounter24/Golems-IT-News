<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorBackgroundLight</item>
        <item name="android:textColorPrimary">@color/descriptionColorLight</item>
        <item name="android:textColorSecondary">@color/descriptionColorLight</item>
    </style>


    <style name="AppThemeDark" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorBackgroundDark</item>
        <item name="android:textColorPrimary">@color/descriptionColorDark</item>
        <item name="android:textColorSecondary">@color/descriptionColorDark</item>
        <item name="android:listDivider">@color/listDividerDark</item>

    </style>

</resources>
